[tool.poetry]
name = "tubetomp3"
version = "0.1.0"
description = ".."
authors = ["yo"]

[tool.poetry.dependencies]
python = "^3.8"
flask = "^2.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"